# 코딩 규칙 (aq2_qrisproject)

이 프로젝트의 코드는 아래 규칙을 따른다. 신규 작성·수정 시 항상 준수하며, 기존 코드도
손대는 김에 규칙에 맞춰 정리한다. 규칙은 인간이 읽고 유지보수하기 좋은 코드를 최우선 목표로 한다.

## 1. OOP · 모듈화

- **책임 하나 = 클래스 하나 = 파일 하나**. 계층별로 디렉터리를 나눈다:
  `data/`(로딩) · `indicator/`(지표) · `strategy/`(전략) · `backtest/`(체결·성과) · `report/`(리포트).
- 각 계층은 **추상 기반 클래스**(`abc.ABC` + `@abstractmethod`)로 인터페이스를 고정하고,
  구현체는 그 인터페이스만 맞춘다. 예: `Indicator.compute()`, `Strategy.generate_signals()`.
- **관심사 분리**를 지킨다. 전략은 신호만 만들고, 체결·비용·성과는 `backtest` 계층이 맡는다.
  한 클래스가 다른 계층의 세부를 알게 되면 잘못 나눈 것이다.
- **의존성 주입**을 기본으로 한다. 협력 객체(로더·지표·엔진)는 생성자 인자로 받아 교체·테스트가
  쉽게 한다. `Pipeline` 처럼 조립은 한곳(오케스트레이터)에 모은다.
- **파라미터는 생성자에서**, 조정값은 `config.py` 의 `@dataclass` 로 모은다.
  코드 수정 없이 `config.json` 값만 바꿔 재실행되도록 유지한다.
- 데이터 묶음은 `@dataclass`(예: `Signals`)로, 가변 전역 상태는 두지 않는다.

## 2. 로깅 (레벨 체계)

- **`print()` 금지, 표준 `logging` 모듈 사용.** 모듈 상단에서
  `logger = logging.getLogger(__name__)` 로 로거를 얻고 이 로거로만 출력한다.
- 로그 초기화(핸들러·포맷·레벨)는 **진입점 한곳**(`main.py`)에서만 한다. 라이브러리성 모듈은
  설정하지 않고 로거만 사용한다(중복 핸들러 방지).
- **레벨을 의미에 맞게** 사용한다:
  - `DEBUG`  — 개발·디버깅용 상세(중간 계산값, 봉 수 등). 평상시엔 안 보임.
  - `INFO`   — 정상 진행 마일스톤(로드 완료, 전략 목록, 종목별 결과, 리포트 경로).
  - `WARNING`— 진행은 되나 주의가 필요(가중 합 ≠ 1.0, 워밍업 부족, 종목 스킵).
  - `ERROR`  — 해당 작업 실패(종목 로드 실패 등). 예외는 `logger.exception()` 으로 트레이스 포함.
  - `CRITICAL`— 파이프라인 전체 중단.
- **포맷은 사람이 읽기 쉽게**: `시각 · 레벨 · 모듈 · 메시지`.
  예: `logging.Formatter("%(asctime)s [%(levelname)-7s] %(name)s: %(message)s", "%H:%M:%S")`
- 표(종목별 결과·유니버스 평균)처럼 **정렬이 중요한 출력은 폭 지정 포맷**을 유지한다
  (`f"{code:<5} {name:<26} {val:>7.1f}%"`).
- 메시지 앞의 `[config]`·`[data]` 같은 수동 태그는 로거 이름(`%(name)s`)으로 대체한다.

## 3. 가독성

- **한글 docstring·주석**을 유지한다(모듈·클래스·공개 메서드에 docstring 필수).
- docstring 은 **무엇을 왜** 하는지 먼저 쓰고, `Args:` / `Returns:` / `Attributes:` 로 계약을 명시한다.
- 섹션 구분선으로 파일 내부 구조를 드러낸다: `# ── 조립 ─────────────`.
- 함수는 한 화면 안에 들어오게 짧게. 깊은 중첩보다 이른 반환(early return)을 쓴다.
- 이름은 의도가 드러나게(`target_long`, `warmup_bars`). 축약·매직넘버는 상수나 config 로 뺀다.

## 4. 주석

- **왜(의도·근거)를 설명**한다. 코드가 그대로 말하는 "무엇"은 반복하지 않는다.
  예: `# EMA 스무딩(whipsaw↓)`, `# i+1 시가 체결 → 룩어헤드 방지`.
- 비직관적 결정, 도메인 규칙(히스테리시스·워밍업·룩어헤드), 트레이드오프에 주석을 남긴다.
- 주석과 코드가 어긋나지 않게, 코드를 고치면 주석·docstring 도 함께 고친다.

## 5. 기타

- 파일 상단에 `from __future__ import annotations` 를 두고 타입 힌트를 붙인다.
- 실행: `python main.py` (설정은 `config.json`, 없으면 내장 기본값 폴백).

---

# 작업 브리핑 (2026-07-15 밤 기준 — 파라미터 동결 완료)

## 프로젝트

- KDT AI퀀트 팀프로젝트 "액티브 ETF 개발". **마감 2026-07-22(수)**.
- 산출물: ① 전략·백테스트 소스코드 ② 투자제안서 PDF 5~15장 ③ 발표.
- 평가: 논리 30 / 리스크 관리 30 / 기술 완성도 20 / 성과·발표 20 → 문서·발표 비중이 절반 이상.
- 상품 컨셉: IRP(연금계좌) 가입자 타깃 하락 방어형 액티브 ETF. 가칭 "HELM 연금올인원액티브"(팀 미확정).
- 3인 팀. 원본 엔진은 승환님 작업 — **원본 파일 수정 금지. 모든 변형은 V2 모듈(신규 파일)로 분리**하고,
  결과는 팀에 보고 후 깃에 올린다.

## 전략 구조

- 채권 3종(153130/114260/273130) 각 10% 고정 = 30% + 사테라이트 70%.
- 사테라이트: 유니버스 **36종**에서 TrendScore 상위 Top-7 월간 로테이션(슬롯당 포트폴리오 10%).
- 게이트 히스테리시스: **진입 52 / 청산 45**. 미달로 빈 슬롯은 단기채권(153130) 대피 → 슬롯 수 자체가 국면 대응.
- **Tier 2-a 부분 진입(동결)**: 슬롯을 이진(60점=100%)이 아니라 점수 비례로 채운다.
  52점 30% → 60점 100%, 60점 이상 만충. 진입·유지 양쪽에 같은 경사(`ramp_hold=True`).
- 분기 + ±7%p 임계 리밸런싱. 벤치마크 KODEX TRF7030(329670).
- TrendScore = EWMAC 앙상블(8/32·16/64·32/128) 0.55 + TSMOM(12M 모멘텀 − 1M 리버설) 0.25
  + RSI 0.20 − ADX 페널티(최대 15). 워밍업 252봉.

## 유니버스 (36종 — 확정)

- 글로벌 9 · 미국섹터 9 · 원자재/리츠 3(구리 160580 · **금 ACE KRX금현물 411060** · 리츠 329200) · 한국섹터 15.
- **금은 411060(현물)만 가능**: IRP 는 파생형(골드선물 ETF) 편입이 막혀 있다. 이전 코드 0072R0
  (TIGER KRX금현물)은 2025-06 상장이라 248봉 < 워밍업 252봉 → 단 한 번도 후보가 못 되는 죽은 티커였다.
- **은 제외**: 현물 은 ETF 자체가 없다(KRX 금시장은 금 전용). TIGER 은액티브(0189B0)는 2026-04 상장.
- **규정**: 신규 상장 종목은 252봉 축적 후 재심사 편입한다. 워밍업 미달 티커를 유니버스에 남기면
  '37종'처럼 실제와 다른 표기가 생긴다(죽은 티커 제외 전후 수치 오차 2e-15 = 완전 동일).

## 기준선 수치 동결 (V2 · 2026-07-15 확정 — 모든 비교의 기준)

- **전체(2020-01~2026-06): CAGR 13.4 / Sharpe 1.18 / MDD −12.7% / Calmar 1.05 / 최저 해 +2.2% / 2022 +2.2%**
- **2025-12-31 컷: CAGR 10.0 / Sharpe 1.22 / MDD −9.2% / Calmar 1.09 / 최저 해 +2.2%**
- 연도별: 2020 +3.5 · 2021 +15.5 · 2022 +2.2 · 2023 +6.8 · 2024 +19.7 · 2025 +13.3 · 2026 +27.5
- 재현: `uv run python run_v2.py` (`FROZEN_RAMP = (52, 60, 0.3)`), 기간 컷은 `--end 2025-12-31`.
- 참고 V1 기준선(36종·이진 60): 전체 13.0 / 1.10 / −12.7 / 1.02 · 2025컷 7.3 / 0.88 / −10.8 / 0.68.

## 실험 동결 (2026-07-15 발효)

**이 시점 이후 파라미터·유니버스 실험 금지.** 남은 기간은 검증 재료 산출과 문서 작업만 한다.

## 실험 규율 (위반 금지 — 문서·발표에서 이 규율 자체가 자산)

1. 판정 기준을 실행 **전에** 고정: Calmar ≥ 기준선 + 최저 해 개선 + 2022년 플러스 방어 유지.
   CAGR 단독 비교 금지. (2022 잣대 논쟁: "≥ 기준선 절반" 정교화안이 제기됐으나 **미채택** —
   +2.2%는 정체성 소멸이 아닌 감쇄이고 최저 해 개선(+0.2→+2.2, "잃는 해 없음")이 상쇄.
   발표 Q&A "2022 방어가 왜 약해졌나" 대비 답변 재료.)
2. 특정 연도/구간을 겨냥한 수정 금지. 수정 근거는 메커니즘 가설이어야 함.
3. 주변값 강건성: 채택하려면 파라미터 그리드에서 plateau 여야 함(한 점만 좋으면 기각).
4. 전체 기간 + 2025년 말 컷 두 구간 모두에서 우위여야 채택.
5. 한 번에 하나만 변경, 결과는 기준선 대비 기록.

## 완료된 실험 (반복 금지)

- **Tier 2-a 부분 진입: 채택(동결).** 진입 문턱 60→52 인하 + 52~60 구간 크기 경사(바닥 30%).
  하한충전율(0.2~0.4)·문턱(48~52)·만충점수(58~62) 3축 그리드 **9점 전부** 두 구간 모두 기준선
  Calmar 초과(전체 1.04~1.13 / 2025컷 0.88~1.09) = 면 전체가 들린 plateau.
  **가설 확인**: 개선된 해가 2020·2023·2025 — 진단서의 '구조적으로 취약한 V자 반등' 3개 연도와 정확히 일치.
- **진입 경사 단독(문턱 50, 보유 만충): 기각.** MDD −17.6%, 2022 −1.5%, Calmar 0.76.
- **유지 축소 단독(문턱 60 고정 + 보유만 축소): 기각.** 2025컷은 Calmar 0.68→0.92 로 크게 개선하나
  (MDD −10.8→−8.0, CAGR 손실 없음) 전체 기간 0.94~0.98 로 기준선 미달 → **국면 편중**.
  reversal_weight 기각과 동일한 실패 패턴.
- **금 411060 슬롯 기회비용 측정.** 편입 시 두 구간 모두 Calmar 미달(전체 1.05→1.02, 2025컷 0.77→0.68).
  58개 교체구간 중 11개 보유, 금 자체 수익은 대부분 플러스였음에도 전체는 악화 — **슬롯이 제로섬**이라
  8위 후보를 밀어낸 기회비용이 더 컸다. 그럼에도 자산군 정합성·실투자 가능성을 위해 채택(사람 결정).
- **smooth_span=30: 기각.** 진입 지연으로 추세장 상실.
- **reversal_weight 0.75/0.5/0.25/0.0: 기각.** 전체 기간 단조 개선이나 2025 컷에서 미달 → 국면 편중.

## 핵심 메커니즘 해명 (제안서·발표 최대 자산)

**TrendScore 60점 문턱의 실제 역할은 품질 게이트가 아니라 '기득권 보호'다.** 45↔60 의 넓은
히스테리시스 덕에 보유 종목은 60 이상을 받은 '압도적으로 나은' 신규에게만 슬롯을 내준다.

2026-04-01 체크 실측(문턱을 50으로 내렸을 때, 교체 1회로 해당 구간 +22.8% → +7.9%):
- 보유 주도주가 조정으로 45~50 대 하락: 증권 50.0 · IT 49.7 · 반도체 49.6 · 은행 48.9
- 신규 방어주가 50~60 대: 경기소비재 59.5 · 운송 58.9 · 미국유틸리티 57.7 · 미국에너지 57.1
- 문턱 60 이면 방어주가 자격 미달 → 주도주 잔류. 문턱 50 이면 방어주가 랭킹에서 주도주를 축출.

**MDD 를 통제하는 것은 오직 이 문턱이다**: 문턱 60 → −12.7 / 문턱 50 + 보유 만충 → −17.6 /
문턱 60 + 보유 축소 → −12.7(축소는 MDD 를 개선하지 못함) / 문턱 50 + 보유 축소 → −12.7(상쇄일 뿐).
→ 동결값이 문턱을 8점만 내린 이유. 원설계(승환님의 60/45)가 옳았음을 확인·확장한 결과다.

**슬롯은 충전율과 무관하게 소비된다** — 20%만 채우는 종목도 슬롯 하나를 차지해 기득권자를 밀어낸다.
그래서 하한충전율을 아주 낮게 잡으면 '노출은 없는데 자리는 뺏는' 최악 조합이 된다.

## 진단된 전략 특성 (제안서 재료)

- 빠른 청산 / 느린 재진입의 의도된 비대칭. V자 반등(2020·2023·2025)에 구조적으로 취약 — 원인 4중:
  TSMOM 리버설 항, 느린 EWMAC 다리(32/128), 급락 후 변동성 정규화 분모 팽창, 전환기 ADX 페널티.
  → **Tier 2-a 동결이 정면으로 겨냥한 지점이며 그 3개 연도가 실제로 개선됐다.**
- 전환 구간(5슬롯대) 평균 구간수익 −1.08% — 유일한 마이너스 상태. 방어 상태(0~3슬롯)는 잃지 않음.
- 78개월 중 62개월 슬롯 미달, 전부 게이트 미달(데이터 공백 0건).
- 기여 분해(동반 보유 대비): 상위 KODEX IT +8.7%p·미국에너지 +6.4·증권 +4.3·반도체 +3.3 /
  하위 구리 −8.4(n=3)·헬스케어 −4.3·건설 −4.2. 동일가중 희석 가설은 기각(슬롯당 10% 고정 구조).
- 구간 손익: 승률 67%, 손익비 1.58, Profit Factor 3.54.

## 기록된 실수 (숨기지 않는다 — 검증 루프가 작동한 사례로 발표 자산)

1. **규율 5 위반**: 경사를 진입·유지에 동시 적용해 두 메커니즘을 한 번에 바꿨다. 분리 측정 전까지
   결과 해석이 불가능했다.
2. **오귀인**: "보유 축소가 MDD 를 지킨다"고 결론냈으나, 분리해보니 문턱 인하 피해를 상쇄한 것뿐이었다.
   MDD 통제는 문턱 단독의 몫.
3. **전달 체인 오염**: 채팅→코드 전달문에서 이전 설계 제안 문구("진입 문턱 60 유지")가 측정 결과의
   서술 위에 잔존해 동결 구성을 잘못 기술. 코드 쪽 실증 대조(문턱 60 구성은 전체 Calmar 0.90 으로
   기각 대상)로 고착 전 차단됨.
4. **검증 없는 근거 서술**: 411060 parquet 커밋의 사유를 "클론 즉시 재현"으로 적었으나, 실제로
   클론해 보니 나머지 39종이 전부 gitignore 라 빈 클론은 이 파일이 있어도 못 돈다. 실제 효용은
   "이미 캐시를 가진 팀원이 pull 했을 때 411060 하나 때문에 막히지 않는 것"뿐. 커밋 메시지 amend.
   → **3번과 같은 종류다: 검증 없이 쓴 근거는 남의 것이든 내 것이든 똑같이 틀리고, 두 번 다 코드
   실증이 잡았다.** 실험 규율이 작동한 사례로 발표 Q&A 에 쓴다.

전체 검증 항목·수치·미검증 한계는 `docs/verification_log.md` 참조.

## 파일 지도 (V2 = 이번 작업분)

- `satellite/backtester_v2.py` — 슬롯 경사 진입. **자격 문턱(entry_gate)·경사 하단(ramp_score)·
  만충점수(full_score)·유지 적용(ramp_hold) 4축 분리.** 축퇴(`ramp_score=None`) 시 원본과 비트 단위 일치.
- `irp/backtester_v2.py` — 사테라이트 슬리브 주입용(원본은 생성자에서 직접 만들어 교체 불가).
- `indicator/trend_score_v2.py` — reversal_weight 파라미터화. rev=1.0 은 원본과 수치 일치.
- `run_v2.py` — V1/V2 비교 러너. `FROZEN_RAMP` 가 동결값, `_PLATEAU_GRID` 가 규율 3 근거 재현용.
  `--start/--end` CLI 로 config 수정 없이 기간 컷(구 방식은 config 를 고쳤다 복원을 잊는 사고가 있었다).
- `data/yfinance_loader.py` — **패치됨**(KRX 6자리 .KS→.KQ 폴백; 원본은 접미사 미부착으로 한국 종목
  다운로드 전멸). 커밋 여부는 팀 보고 후 결정.

## 환경 주의사항

- 패키지: `uv` 사용 (`uv run python ...`). Python 3.13.
- 데이터: `datasets/ohlcv/` parquet 캐시. `config.json` 의 data.source 는 **`parquet` 유지**
  (로컬에서 `yfinance` 로 바뀐 채 방치된 사고가 있었다 — 커밋 금지 대상).
- yfinance 로 새로 받을 땐 macOS 에서 `ulimit -n 4096` 선행 필수(FD 고갈로 죽음). 또한
  **`start` 를 반드시 명시**할 것 — `start=None` 이면 yfinance 가 기본 1개월치만 준다(23봉 함정).
- 리포트는 `reports/` 에 생성.

## 다음 작업 (우선순위)

1. **적립식 모듈** — 동결 V2 의 일간 equity 위에 월 납입 현금흐름(`analysis/` 신규, 엔진 무수정).
   시나리오 ① 월 75만 적립(연 900만) ② 연초 일시납 900만 — 총 납입액 대비 최종 평가액 + MWR(IRR) 병기.
   롤링 시작점(모든 월) 분포 + 보유기간별 손실 확률 곡선. 벤치마크(TRF7030)에도 동일 적용해 병렬 제시.
   산출물: `reports/` CSV + 차트(한글 폰트 `koreanize_matplotlib`), 제안서 §8~9 형태로.
2. **제안서 재료 산출** — 위 메커니즘 해명·부정적 결과·규율을 문서화.
3. ~~Tier 2-b 확신도 가중~~ → **향후 과제로 강등**(실험 동결).
