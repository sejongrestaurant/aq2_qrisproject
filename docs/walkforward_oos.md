# OOS 워크포워드 검증 — "동결 수치가 커브피팅인가"

작성 2026-07-22. 대상: 동결 V2(`FROZEN_RAMP = (52, 60, 0.3)`).
재현: `python run_walkforward.py` (롤링 대조 `--scheme rolling`).

이 문서는 **무엇을 어떻게 확인했는지**의 기록이다. 통과한 것만이 아니라 **미달 항목과 그
해석까지 전부 남긴다** — `verification_log.md` 와 같은 원칙이다.

---

## 0. 이 검증은 상품을 바꾸지 않는다

2026-07-15 실험 동결이 발효 중이다. 워크포워드가 창마다 어떤 파라미터를 골랐든 **동결값은
그대로다.** 여기서 고르는 후보는 "그 시점에 이 규칙을 썼다면 무엇을 골랐을까"의 재현이지
채택 후보가 아니다. 산출물은 실험 결과가 아니라 **검증 기록**이므로 동결 규율 위반이 아니다.

---

## 1. 무엇을 물었나

기준선 수치(전체 CAGR 13.6 · Calmar 1.07)는 2020-01~2026-06 **전 구간을 보고** 고른
파라미터의 성적이다. 그리드 9점이 전부 기준선을 넘겼다(plateau)는 근거가 있었지만, 그
그리드 **자체를 같은 구간에서** 평가했으므로 "표본 밖에서도 그런가"는 답이 안 된 질문이었다.

워크포워드는 선정을 학습 창 안에 가둔다:

| 창 | 학습(선정에만 사용) | 검증(선정에 한 번도 쓰이지 않음) |
|---|---|---|
| 0 | 2020-01~2021-12 (24개월) | **2022** |
| 1 | 2020-01~2022-12 (36개월) | **2023** |
| 2 | 2020-01~2023-12 (48개월) | **2024** |
| 3 | 2020-01~2024-12 (60개월) | **2025** |
| 4 | 2020-01~2025-12 (72개월) | **2026-01~06** |

검증 창은 겹치지 않으므로 이어붙이면 연속된 OOS 구간(2022-01~2026-06)이 된다.

**검증 대상은 Tier 2-a 슬롯 경사 3축**(경사 하단 · 만충 점수 · 하한 충전율, 45조합)
**+ 문턱60 이진 구성(원설계)**(귀무가설로 후보에 포함) = 46후보.
(코드·CSV 상의 라벨은 `V1 이진60` 이다.)
**검증 대상이 아닌 것:** top_n=7, 히스테리시스 60/45, TrendScore 가중치, 채권 30%,
유니버스 36종. 이들도 전 구간을 보고 정했으므로 이 검증 **밖**에 있다.

---

## 2. 결과 — 사전 등록 기준 6개 중 4개 통과

임계값은 실행 **전에** `validation/verdict.py` 에 적어 넣었고 결과를 보고 옮기지 않았다.

| 기준 | 실측 | 임계 | 판정 |
|---|---|---|---|
| 표본 밖 생존 (OOS/IS Calmar) | 1.19 / 1.07 = **1.11** | ≥ 0.70 | 통과 |
| 동결값 검증 (\|WF − 동결\|) | \|1.19 − 1.22\| = **0.03** | ≤ 0.20 | 통과 |
| 선정 유용성 (vs 격자 평균) | 1.19 vs **1.25** | ≥ 격자평균 | **미달** |
| 선정 비용 (vs 오라클) | 1.19 / 1.60 = **0.75** | ≥ 0.60 | 통과 |
| 벤치마크 우위 (창별 승률) | **40%** | ≥ 60% | **미달** |
| 잃는 해 없음 (OOS 최저 해) | **+2.4%** | ≥ 0% | 통과 |

### 이어붙인 OOS 곡선 (2022-01~2026-06)

| 곡선 | CAGR% | Sharpe | MDD% | Calmar | 최저 해% |
|---|---|---|---|---|---|
| 워크포워드 OOS | 15.1 | 1.25 | −12.7 | 1.19 | +2.4 |
| 동결 52→60·30% | 15.5 | 1.26 | −12.7 | 1.22 | +2.2 |
| 문턱60 이진(원설계) | 16.7 | 1.28 | −12.7 | **1.31** | +4.3 |
| 격자 평균 | 15.9 | 1.27 | −12.7 | 1.25 | +1.6 |
| 오라클(사후최적) | 20.3 | 1.53 | −12.7 | 1.60 | +5.8 |
| 벤치마크 TRF7030 | 12.6 | 1.17 | −13.6 | 0.93 | **−11.3** |

세 선정 규칙(plateau Calmar · 최대 Calmar · 최대 Sharpe) 모두 4/6 통과로 결론이 같다.
롤링 고정폭 창(`--scheme rolling`)도 같은 두 항목에서 미달 — **결론은 창 설계의 산물이 아니다.**

---

## 3. 발견 — 제안서·발표에 쓸 것

### 3-1. 동결값은 "평평한 면 위의 한 점"이 맞다 (표본 밖에서 재확인)

WF 곡선과 동결 곡선의 Calmar 차이가 **0.03**이다. 창마다 다시 골랐는데도 동결값을 계속
쓴 것과 거의 같은 결과가 나왔다 — plateau 주장이 **선정에 쓰이지 않은 구간에서** 성립한다.
`walkforward_curves.png` 에서 두 곡선이 겹쳐 보이는 것이 이 문단의 그림이다.

### 3-2. 그런데 "평평하다"는 강세장 얘기다 — 약세장에서는 선택이 갈린다

`walkforward_spread.png` 가 이 발견의 그림이다. 창별로 후보의 OOS Calmar 를 그 창의
중앙값 대비 배수로 뿌리면(격자 45조합과 문턱60 구성을 분리해서 읽어야 한다):

| 창 | 창 중앙값 Calmar | Tier 2-a 격자 45조합 배수 | 문턱60 구성 배수 |
|---|---|---|---|
| 2022 | 0.34 | **0.43 ~ 2.02** | 3.80 |
| 2023 | 1.34 | 0.80 ~ 1.12 | 0.39 |
| 2024 | 2.76 | 0.97 ~ 1.01 | 0.86 |
| 2025 | 1.50 | 0.93 ~ 1.04 | 0.41 |
| 2026 상반 | 5.31 | 0.99 ~ 1.00 | 1.72 |

- **2023~2026 — 격자 구름이 0.8~1.1배에 뭉친다.** 무엇을 고르든 결과가 사실상 같다.
- **2022(약세장) — 0.43~2.02배로 4.7배 벌어진다.** 파라미터 선택이 성과를 크게 가른다.
- 문턱60 구성은 격자 밖(문턱이 8점 높다)이라 늘 구름 바깥에 있다(2022·2026 위, 2023·2025 아래) — §3-4.

즉 plateau 는 **강세 국면의 성질**이고, 정작 파라미터가 중요한 국면은 약세장이다.
인샘플 그리드가 평평해 보였던 것은 전 구간이 강세장에 지배됐기 때문이다.
**"파라미터에 둔감한 전략"이라는 서술은 과하다** — "평시엔 둔감하고 약세장엔 민감하다"가 맞다.

### 3-3. 학습 창 성적으로 고르는 것은 도움이 되지 않았다

선정된 후보의 검증 창 등수: 3/46 · 31/46 · 45/46 · 41/46 · 46/46 (평균 33/46).
학습 창 성적이 **다음 구간의 순위를 예측하지 못한다.** 다섯 창 전부에서 학습 Calmar 는
하한충전율 **0.20**(격자 가장자리)을 뽑았고, 동결값은 0.30 이다.

**다만 등수는 겉보기보다 훨씬 덜 심각하다** — 후보들이 거의 붙어 있어 등수만 갈린다:

| 채점 창 | 선정 후보 Calmar | 격자 45조합 범위 | 등수 |
|---|---|---|---|
| 2022 | 0.68 | 0.15 ~ 0.69 | 3 |
| 2023 | 1.29 | 1.07 ~ 1.50 | 31 |
| 2024 | 2.68 | **2.68 ~ 2.78** | 45 |
| 2025 | 1.43 | 1.39 ~ 1.56 | 41 |
| 2026 상반 | 5.25 | **5.25 ~ 5.32** | 46 |

2024·2026 은 '꼴등'이지만 1등과의 차이가 0.10·0.07 이다. 즉 학습 성적으로 고르는 것은
**도움도 손해도 되지 않는다** — §3-1(면이 평평하다)의 다른 표현이다.

#### 3-3-1. 하한 충전율에는 '정답'이 없다 — 국면에 따라 방향이 뒤집힌다

창별 평균 구간수익(%)을 하한충전율로 갈라 보면 부호가 뒤집힌다:

| 하한 충전율 | **2022(하락)** | 2023 | 2024 | 2025 | 2026 상반 |
|---|---|---|---|---|---|
| 0.20 (적게) | **+1.88** | 7.89 | 19.82 | 13.43 | 29.2 |
| **0.30 (동결)** | +1.55 | 8.21 | 19.87 | 13.81 | 29.2 |
| 0.40 (많이) | **+1.22** | **8.53** | **19.92** | **14.20** | 29.2 |

- **하락장에선 적게 실을수록**(0.20 이 0.40 보다 +0.66%p), **상승장에선 많이 실을수록** 유리하다.
- **2026 상반기엔 차이가 0.00** 이다 — 점수가 전부 만충 구간(60점 이상)이라 경사를 아예
  지나가지 않는다. 이 사실이 §3-4 의 원인 규명에 결정적으로 쓰인다.

OOS 전체 평균으로 줄 세우면 0.40 이 1위(Calmar 1.253), 동결값 0.30 은 3위(1.246)로 중간이다.
그러나 그 평균은 **상승장 4년 + 하락장 1년**이라는 구간 구성이 만든 값이라 '정답'이 아니다.
동결값 0.30 의 의미는 "가장 좋은 값"이 아니라 **"어느 국면에도 크게 틀리지 않는 중간"**이다.

> **철회:** 이 문서 초판의 *"동결이 면 한가운데 0.30 을 고른 판단이 결과적으로 옳았다"* 는
> 서술은 **과장이라 철회한다.** 0.30 과 0.20 의 평균 Calmar 차이는 0.007 로, 우열을 말할
> 크기가 아니다. (plateau 규칙도 0.20 을 뽑았다 — 이웃 평균으로도 걸러지지 않았다.)

### 3-4. 문턱을 60 → 52 로 내린 대가가 강한 추세 구간에서 크다

격자 밖 대조군으로 **문턱60 이진 구성(원설계)** 을 함께 돌렸다(코드·CSV 라벨은 `V1 이진60`).
OOS 구간 합산은 원설계 Calmar **1.31** > 동결 **1.22** 로, 연도별로 방향이 갈린다:

| 연도 | 동결(문턱52·경사) | 문턱60 이진(원설계) | 우세 |
|---|---|---|---|
| 2022 | +2.2 | +5.8 | 문턱60 |
| 2023 | +6.8 | +4.3 | 동결 |
| 2024 | +19.7 | +15.9 | 동결 |
| 2025 | +13.3 | +6.6 | 동결 |
| 2026 상반 | +29.3 | **+46.9** | 문턱60 |

Tier 2-a 가 겨냥한 V자 반등 연도(2023·2025)는 **의도대로 개선**됐다. 합산 순위를 뒤집는 것은
사실상 **2026 상반기 한 구간(+29.3 vs +46.9, 17.6%p)** 단독이다.

**원인은 부분 충전이 아니라 문턱이다.** §3-3-1 에서 2026 상반기의 하한충전율 효과가 **정확히
0.00** 이었다 — 그 구간엔 점수가 전부 만충 구간이라 경사를 지나가지 않는다. 실제로 격자
45조합이 문턱(48~52)과 무관하게 전부 ~29.2% 로 뭉쳤고, 문턱 60 만 46.9% 로 튄다. 즉 이
격차는 **문턱을 60 에서 52 로 내려 '그저 그런 신규'에게 편입 자격을 준 대가**이며, 팀이 이미
문서화한 *"문턱 60 의 실제 역할은 품질 게이트가 아니라 기득권 보호"* 와 같은 메커니즘이다
(2026-04 실측: 교체 1회로 해당 구간 +22.8% → +7.9%).

**해석 주의 — 되돌리자는 뜻이 아니다.** ① 2020(Tier 2-a 의 최대 근거 연도)이 학습 창이라
OOS 집계에서 빠져 있고, ② 순위가 반년짜리 한 구간에 걸려 있으며, ③ 무엇보다 **구간 성적을
보고 파라미터를 되돌리는 것이 바로 §3-3 이 반증한 행동**이다(학습 창 순위는 다음 구간을
예측하지 못한다).

다만 **"Tier 2-a 가 전 구간에서 낫다"는 서술은 이제 쓸 수 없다.** 정확한 서술은
*"Tier 2-a 는 V자 반등 국면(2023·2025)을 겨냥해 그 국면을 개선했고, 그 대가로 강한 추세
구간에서는 낮아진 문턱이 주도주를 교체시켜 상승을 덜 먹는다"* 이다. 발표 Q&A 대비 재료.

### 3-5. 최대낙폭 −12.7% 는 파라미터로 방어되지 않는 단일 사건이다

WF·동결·문턱60·격자평균·오라클 **다섯 곡선의 MDD 가 전부 −12.7%**, 고점 2026-02-26 →
저점 2026-03-04 로 **동일하다**. 일주일짜리 급락이라 월간 로테이션이 반응할 시간이 없고,
그 시점 모든 변형이 사실상 같은 상위 7종을 들고 있었다.

→ 게이트가 방어하는 것은 **느린 약세장(2022)**이지 **일주일 급락**이 아니다. 리스크 관리
섹션에 이 구분을 명시하는 편이 안전하다(평가 항목 '리스크 관리 30점').

### 3-6. 벤치마크 승률 40% 는 상품 성격 그대로다

창별 Calmar 승률은 2/5 다. 그러나 이어붙인 곡선에서는 Calmar 1.19 vs 0.93,
최저 해 **+2.4% vs −11.3%** 로 앞선다. 벤치마크(주식 70%)는 강세 연도(2023·2024)에
크게 이기고 약세 연도(2022)에 크게 진다. **"항상 이긴다"가 아니라 "덜 잃는다"**가 상품의
주장이므로 이 미달은 설계와 모순되지 않는다. 다만 등록 기준은 미달로 그대로 남긴다.

보조 지표 승률(같은 창): CAGR 40% · Sharpe 40% · **MDD 60%**.

---

## 4. 방법론 한계 (측정하지 않은 것)

1. **검증 구간도 과거다.** 여기서 'OOS'는 "파라미터 선정에 쓰이지 않은 구간"이라는 뜻이지
   미래가 아니다. 유니버스·상장 이력·데이터 자체는 전부 사후에 확정된 것이다.
2. **창이 5개뿐**이라 승률의 표준오차가 크다. 40% 와 60% 를 통계적으로 구분할 수 없다.
3. **설계 대부분이 검증 밖에 있다**(§1). 이 검증은 '동결 선정 단계'만 다룬다.
4. **창 경계의 상태 인계를 흉내 냈다.** 각 후보를 전 구간 한 번 돌려 창마다 잘랐으므로,
   창이 바뀔 때 직전 파라미터로 굴러온 포지션을 새 파라미터로 재조정하는 과정이 실제와
   다르다. 월간 로테이션이라 한 달이면 씻기고 경계 회전율은 `switch_cost`(왕복 0.10%)로
   대략 물렸지만, 정확한 값은 아니다. 엔진에 상태 인계 자리가 없어 한계로 남긴다.
5. **창 지표의 Calmar 는 불안정하다.** 1년 창에서 분모(그 해 낙폭)가 얕으면 폭발한다
   (2026 상반 창의 벤치마크 Calmar 5.3). 낙폭의 절대 크기는 반드시 **이어붙인 곡선**에서
   읽어야 한다.

---

## 5. 부수 발견 (본 작업 중)

- **기준선 수치가 CLAUDE.md 기록과 어긋난다.** 현재 재현값은 동결 CAGR **13.6** /
  Calmar **1.07**, 문턱60 구성 **13.3** / **1.04** 인데 CLAUDE.md 기록은 13.4/1.05, 13.0/1.02 다.
  두 구성이 같은 방향·같은 폭으로 움직였으므로 로직 변경이 아니라 **시세 데이터 갱신**으로
  보인다. `run_v2.py` 직접 실행과 본 검증 하니스가 서로 일치하므로 하니스 문제는 아니다.
  → 동결 기록 수치를 갱신할지 팀 결정 필요.
- **차트 한글 폰트가 Windows 에서 깨져 있었다.** `analysis/fonts.py` 의 폴백이
  `AppleGothic`(macOS 전용) 하나뿐이라, Windows 팀원 환경에서는 **없는 폰트를 지정한 채
  경고만 남고 그림이 나왔다**(한글 전부 □). 설치된 폰트를 실제로 찾아 잡도록 고쳤다
  (`Malgun Gothic`·`NanumGothic` 등 폴백 목록).
- **`irp/backtester.py:124` 에 잠복 결함.** `_simulate` 시그니처가 `Optional[float]` 을
  쓰는데 `typing.Optional` 이 임포트돼 있지 않다. `from __future__ import annotations` 덕에
  런타임에는 문자열로 남아 터지지 않지만, `typing.get_type_hints()` 를 부르는 순간 깨진다.
  원본 무수정 원칙이라 **손대지 않고 기록만** 남긴다.

---

## 6. 산출물

| 파일 | 내용 |
|---|---|
| `reports/walkforward_summary.csv` | 규칙별 이어붙인 OOS 지표 + 비교 곡선 |
| `reports/walkforward_folds.csv` | 창별 선정·학습 성적·OOS 성적·검증 등수 |
| `reports/walkforward_yearly.csv` | OOS 연도별 수익(§3-4 표의 원자료) |
| `reports/walkforward_verdict.csv` | 사전 등록 기준별 통과/미달 |
| `reports/walkforward_oos.csv` | 이어붙인 OOS 일간 곡선(원자료) |
| `reports/walkforward_curves.png` | OOS 곡선 vs 동결 vs 벤치(§3-1 의 그림) |
| `reports/walkforward_folds.png` | 창별 OOS Calmar 비교 |
| `reports/walkforward_spread.png` | 창별 후보 분포(§3-2 의 그림) |

코드: `validation/` 계층 8모듈 + `run_walkforward.py`. 엔진은 수정하지 않았다
(로더·지표는 메모이제이션 래퍼를 **주입**해 재계산만 줄였다).
