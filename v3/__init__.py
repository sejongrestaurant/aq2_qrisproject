"""V3 탐색 배터리 — 동결 V2 위에 얹는 변형 실험 모듈 모음(원본·V2 무수정).

이 패키지의 모든 코드는 **읽기 전용 실험**이다. `config/irp.json`·동결 커밋·원본 엔진·V2
모듈을 일절 건드리지 않고, 로드한 설정을 deepcopy 해 메모리에서만 변형한다. 판정(채택/기각)은
하지 않는다 — 사전 고정한 관문 4개의 통과 여부와 예상 실패 모드의 실현 여부만 숫자로 낸다.

구성:
  · measure   — 공통 측정 하버스(전지표 + 연도별 + 관문 평가 + 보고).
  · bond_switch  — [A-2] 채권 슬리브 추세 스위칭.
  · overlay      — [B-1] 변동성 타깃 오버레이.
  · irp_trailing — [C-1] 사테라이트 슬리브 트레일링 스탑 활성화.
  · regime_bond  — [C-2] 국면 연동 채권비중.
  · regional_cap — [C-3] 지역 집중 상한.
"""
