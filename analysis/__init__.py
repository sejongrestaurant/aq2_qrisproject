"""분석 계층 — 백테스트 산출물(자산곡선) 위에 얹는 사후 분석.

전략·체결 엔진의 **로직은 일절 건드리지 않는다**. 대부분의 모듈은 이미 만들어진 일간 자산곡선
(`BacktestResult.equity`)만 입력으로 받아, 투자자 관점의 질문에 답한다:
"매달 75만 원씩 넣었으면 실제로 얼마가 됐나?", "언제 시작했느냐에 따라 얼마나 갈리나?",
"몇 년을 들고 있어야 잃을 확률이 사라지나?"

예외는 `exposure` 다. "얼마나 투자돼 있었나"는 자산곡선에 남지 않는다(곡선은 결과일 뿐 노출을
말해 주지 않는다). 그래서 이것만은 엔진 안에서 **읽는다** — 백테스터를 상속해 이미 정해진
목표비중을 받아 적을 뿐, 판정에는 관여하지 않는다. 밖에서 점수·히스테리시스를 재구현해 재는
방법도 있으나 그건 엔진과 어긋날 수 있고, 실제로 구 `universe_availability.py` 가 그렇게 어긋났다.

구성:
  · `cashflow` — 납입 계획(월 적립 / 연초 일시납)을 현금흐름으로 만든다.
  · `irr`      — 불규칙 현금흐름의 내부수익률(XIRR).
  · `dca`      — 자산곡선 + 납입 계획 → 최종 평가액·MWR.
  · `rolling`  — 시작 시점을 굴려 성과 분포·손실 확률을 낸다.
  · `exposure` — 엔진 관측 프로브 → 체크 시점별 실효 노출률과 그 원인.
  · `segments` — 로테이션 기록 → 구간 상태별(슬롯 수) 손익·승률·PF.
  · `benchmark` — 벤치마크 병기(TRF7030·KOSPI200) 절대지표 + 추적오차·정보비율.
  · `turnover` — 비용 있음/없음 대조로 엔진이 청구한 회전율을 되찾아 비용 드래그를 낸다.
  · `regime`   — KOSPI200 200MA 국면(상승·하락·횡보)별 성과 분해. 사후 라벨(신호 아님).
  · `frozen`   — 동결 V2 / V1 기준선 백테스터 조립을 한곳에(러너 간 구성 불일치 방지).
  · `regime_report` / `turnover_report` — 위 둘의 전시물(국면 음영 자산곡선 · 연도별 회전율).
  · `fonts`    — 차트 한글 폰트·백엔드 설정(한곳에서만).
  · `report_base` — 리포트 저장 공통부(디렉터리·차트·CSV). report/exposure_report 가 상속.
  · `report`   — 적립식 CSV·차트 산출.
  · `exposure_report` — 실효 노출률 CSV·차트 산출.
"""
